/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Linktal Recruitment API
 * API for Linktal Recruitment OS
 * OpenAPI spec version: 1.0
 */
import type { ResolvedValueKind } from './resolvedValueKind';
import type { ResolvedValueRaw } from './resolvedValueRaw';

export interface ResolvedValue {
  /**
     * Exactly what is stored — never replaced, even when a label is available
     * @nullable
     */
  raw: ResolvedValueRaw;
  /**
     * Human label; null when the raw value is null, or when an id could not be resolved (e.g. a hard-purged record)
     * @nullable
     */
  label: string | null;
  /** "fk" = raw was an id resolved via targetType; "enum" = raw was a schema enum value; "plain" = shown as-is */
  kind: ResolvedValueKind;
  /** The model the id was resolved against (e.g. "Industry") — enables deep-linking from the UI. Only present when kind is "fk". */
  targetType?: string;
  /** True when the resolved fk target is soft-deleted */
  deleted?: boolean;
  /** True when this value was suppressed (e.g. Consultant salary/costTo) rather than shown, regardless of raw */
  redacted?: boolean;
}
