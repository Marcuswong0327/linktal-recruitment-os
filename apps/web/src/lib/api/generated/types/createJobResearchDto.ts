/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Linktal Recruitment API
 * API for Linktal Recruitment OS
 * OpenAPI spec version: 1.0
 */
import type { CreateJobResearchDtoStatus } from './createJobResearchDtoStatus';

export interface CreateJobResearchDto {
  /** Client ID the ad was placed by */
  clientId: string;
  /** Consultant who conducted the research. Usually a researcher, but not restricted to that role. */
  consultantId?: string;
  /** Most specific known Location node for the ad (see /locations) — replaces the sheet’s separate "City Advertised"/"Suburbs" columns */
  locationId?: string;
  /** Job title ID (see /job-titles) — the advertiser’s own words */
  jobTitleId?: string;
  /** Job role type ID (see /job-role-types) — the consultant’s classification of the ad */
  jobRoleTypeId?: string;
  /** Snapshot of the client’s status when this research was logged */
  status?: CreateJobResearchDtoStatus;
  /** Link to the Seek listing */
  seekUrl?: string;
  /** Permanent/archived link to the ad, if the original expires */
  permanentUrl?: string;
  /** When the ad was posted (ISO 8601) */
  postedDate?: string;
  /** Contact email scraped from the ad */
  contactEmailFromAd?: string;
  /** Salary as written in the ad. Free text, copied verbatim. */
  salaryRange?: string;
  /** Whether the advertiser has been approached about this ad. Prefer POST /job-research/:id/mark-contacted, which also records who and when; this is here for corrections. */
  isContacted?: boolean;
  /** When the research was logged (ISO 8601). Defaults to now. */
  researchedAt?: string;
  /** Free-text notes on the ad */
  notes?: string;
}
